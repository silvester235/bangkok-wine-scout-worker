{
  "name": "bangkok-wine-scout-worker",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "delete:test-event": "tsx scripts/delete-test-event.ts",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.114.0"
  }
}
